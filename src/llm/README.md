> Thought: We start with some X, we pass that into the machine defined here that is the mapper from X to Y - we run that on a server periodically and over time as F spits out $y_n$ samples of Y we build a queue $Y_^\tilda$ that is an estimate of the true space Y. From that queue we can theoretically infinitely sample a subset either in order or randomly at any subset side $s$. This way we can creat a sort of infinite generator over X.
